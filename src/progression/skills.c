#include "../include/library.h"

skill_info *first_class_skill(char *class, player_info *player_information)
{
    if (strcmp(class, "Warrior") == 0) {
        printf("warrior skill");
        return NULL;
    }
    printf("not finished");
    return NULL;
}